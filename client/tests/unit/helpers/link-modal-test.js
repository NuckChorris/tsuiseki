import {
  linkModal
} from 'tsuiseki/helpers/link-modal';

module('LinkModalHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = linkModal(42);
  ok(result);
});
